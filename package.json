{
  "name": "shopping-cart",
  "version": "1.0.0",
  "scripts": {
    "remove": "sls remove",
    "deploy": "sls deploy",
    "deploy:users-create": "sls deploy function -f users-create",
    "deploy:users-list": "sls deploy function -f users-list",
    "deploy:users-item": "sls deploy function -f users-item",
    "deploy:users-update": "sls deploy function -f users-update",
    "clean": "sls requirements clean && sls requirements cleanCache"
  },
  "dependencies": {
    "aws-sdk": "^2.1381.0",
    "bluebird": "^3.7.2",
    "uuid": "^9.0.0"
  }
}
